# rename this file to opusvl::website.yml and put a ':' after 'name' if
# you want to use YAML like in old versions of Catalyst
name {{ $dist->name =~ s/-/::/gr }}

<Model::CMS>
    connect_info dbi:SQLite:{{ lc $dist->name }}.db
    connect_info username
    connect_info password
</Model::CMS>

<Model::FB11AuthDB>
    connect_info dbi:SQLite:{{ lc $dist->name }}.db
    connect_info username
    connect_info password
</Model::FB11AuthDB>
